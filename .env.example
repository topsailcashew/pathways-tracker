# Google Gemini AI Configuration
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Application Configuration
VITE_APP_NAME=Pathway Tracker
VITE_APP_ENV=development

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the actual .env file to version control
# 2. In production, move AI API calls to a backend server
# 3. The API key should NEVER be exposed in frontend code
# 4. Use server-side proxies for all sensitive API calls

# Feature Flags (for gradual rollout)
VITE_ENABLE_AI_FEATURES=true
VITE_ENABLE_AUTO_WELCOME=false
VITE_ENABLE_GOOGLE_SHEETS_INTEGRATION=false

# API URLs (when you add a backend)
# VITE_API_BASE_URL=http://localhost:4000/api
# VITE_API_TIMEOUT=30000
