# Application Configuration
VITE_APP_NAME=Pathway Tracker
VITE_APP_ENV=development

# Backend API Configuration
VITE_API_URL=http://localhost:4000

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the actual .env file to version control
# 2. AI API calls are now proxied through the backend server
# 3. API keys are stored server-side only
# 4. All sensitive operations go through authenticated API endpoints

# Feature Flags (for gradual rollout)
VITE_ENABLE_AI_FEATURES=true
VITE_ENABLE_AUTO_WELCOME=false
VITE_ENABLE_GOOGLE_SHEETS_INTEGRATION=false
