{
  "name": "pathways-tracker-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Church integration platform for tracking newcomers and new believers with AI-powered insights",
  "author": "Your Church Tech Team",
  "license": "MIT",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=pathway-tracker",
    "dev:web": "npm run dev --workspace=pathway-tracker",
    "dev:api": "npm run dev --workspace=pathways-tracker-backend",
    "dev:all": "(npm run dev --workspace=pathways-tracker-backend &) && npm run dev --workspace=pathway-tracker",
    "build": "npm run build --workspaces",
    "build:web": "npm run build --workspace=pathway-tracker",
    "build:api": "npm run build --workspace=pathways-tracker-backend",
    "test": "npm run test --workspaces --if-present",
    "test:web": "npm run test --workspace=pathway-tracker --if-present",
    "test:api": "npm run test --workspace=pathways-tracker-backend",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present",
    "format": "npm run format --workspaces --if-present",
    "format:check": "npm run format:check --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
